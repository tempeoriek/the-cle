@using Nop.Web.Models.Catalog;
@model PopularProductTagsModel



@if (Model != null)
{
  
        <h3>Concern</h3>
        if (Model.Tags.Count > 0)
        {
        <ul class="list-unstyled sub-cat">
            @foreach (var item in Model.Tags)
                {
                <li>
                    @*<a href="@Url.RouteUrl("ProductsByTag", new { productTagId = item.Id, SeName = item.SeName })" class="producttag" style="font-size:@(Model.GetFontSize(item))%;">@item.Name</a>*@
                    <a href="@Url.RouteUrl("ProductsByTag", new { productTagId = item.Id, SeName = item.SeName })">@item.Name</a>
                </li>
            }
        </ul>
        }
  
}

