@model IList<ProductOverviewModel>

@using Nop.Web.Models.Catalog;

@if (Model.Count > 0)
{
 

    @*<div class="container">
        <div class="row mt-5">
            <div class="col-6 col-md-3 text-left"><h1 class="text-uppercase">Best seller</h1></div>
            <div class="col-md-6 d-none d-md-block"></div>
            <div class="col-6 col-md-3 text-right"><h1 class="text-capitalize"><a href="~/shop">shop all</a></h1></div>
        </div>
        
        <div class="row  mt-5">
            @foreach (var item in Model)
            {
                <div class="col-6 col-md-3 text-center homepage__bestseller">
                    @Html.Partial("_ProductBox", item)
                </div>
            }
        </div>
    </div>*@



    <div class="container homepage-products pt-md-5">
        <h1 class="text-center mb-3 text-uppercase">Shop gifts for every occassion</h1>
        <div class="row">

            @foreach (var item in Model)
            {
                <div class="col-6 col-md-3 homepage__bestseller">
                    @Html.Partial("_ProductBox", item)
                </div>
            }


            @*<div class="col-6 col-md-3">
                <img src="~/Content/Images/uploaded/product/Shop our currated gift 1.png" class="img-fluid" />
                <p class="text-center mt-20">
                    <h3>Eunoia x Cle Spray</h3>
                    <span>IDR 220.000</span><br />

                </p>
            </div>*@
            
        </div><br />
        <a href="~/for-them" class="btn btn-link">See more gifts >></a>
    </div>




}



